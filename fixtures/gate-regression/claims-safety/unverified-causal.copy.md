# FM.UNVERIFIED_CAUSAL — positive ADVISORY (LLM-judged, not executed in CI)

Brand: Acme Cosmos. Both facts below are individually verified in project-context:
(1) the three-day beta ran last weekend; (2) the community Discord added members that week.

The Acme Cosmos beta ran last weekend, and that is exactly why the community grew so fast that
same week.

---

Why a calibrated judge fires `FM.UNVERIFIED_CAUSAL`: an interpretive/causal link ("that is
exactly why") between two facts that are EACH verified, but the causal connection itself is not
established. ADVISORY — it does NOT bar the Recommended slot and does NOT fail the variant; it
rides onto the approval card as a note for the reviewer (§14.4 advisory handling). The verdict is
therefore PASS. If either endpoint were unverified it would be FM.FABRICATION (hard) instead.

# FM.WEAK_ARG — positive (LLM-judged, not executed in CI)

Brand: Acme Cosmos. Content class: an opinion / thesis / commentary piece.

Open betas are the future. Closed betas are outdated. Everyone should run open betas. They're
just better. The data speaks for itself. This is how it should be done.

---

Why a calibrated judge fires `FM.WEAK_ARG`: an opinion/thesis piece that assertion-stacks
("they're just better", "the data speaks for itself") rather than building a complete argument
spine — no claim+mechanism, no verified anchor, no stake, no reframe. SOFT (`warn`), bars the
Recommended slot (RD-21).

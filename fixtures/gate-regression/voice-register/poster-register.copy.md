# FM.POSTER_REGISTER — positive (LLM-judged, not executed in CI)

Brand: Acme Cosmos. Synthetic prose authored fresh for the gate-regression corpus.

The Acme Cosmos beta wrapped this weekend. Remember: the only limit is the one you set for
yourself. Dream big, build bigger, and never stop reaching for the stars.

---

Why a calibrated judge fires `FM.POSTER_REGISTER`: the closing line is a motivational-poster maxim
("the only limit is the one you set for yourself" / "never stop reaching for the stars") rather
than operator-conviction grounded in the piece's verified anchor (the beta). SOFT (`warn`), bars
the Recommended slot (RD-21).
